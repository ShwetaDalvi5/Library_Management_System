import { ReturnBookRequest } from './return-book-request';

describe('ReturnBookRequest', () => {
  it('should create an instance', () => {
    expect(new ReturnBookRequest()).toBeTruthy();
  });
});
